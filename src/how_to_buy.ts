import { NS } from '@ns'

export async function main(ns : NS) : Promise<void> {
    ns.tprint('alias buyOpeners="buy bruteSSH.exe ; buy FTPcrack.exe ; buy relaySMTP.exe ; buy HTTPworm.exe ; buy SQLinject.exe"')
}